{
  "name": "ecom_app",
  "version": "1.0.0",
  "description": "E-com for all",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd x10store && yarn install",
    "dev": "cd x10store && yarn dev",
    "build": "cd x10store && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.12"
  }
}
